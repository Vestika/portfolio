name: Keep Warm

on:
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Cloud Run
        run: curl -sf -o /dev/null -w "%{http_code}\n" https://api.vestika.io/health
